<?xml version="1.0"?>
<project name="mismatched_uses" default="dist">

  <property file="build.properties"/>
  <import file="../build.xml"/>

  <target name="compile" depends="api,common.compile"/>

  <target name="local.pde"/>

</project>
